{
  "name": "gTest",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "author": "",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "lint": "turbo run lint",
    "generate:corebe-endpoints": "node scripts/generate-corebe-endpoints.js",
    "generate:notif-endpoints": "node scripts/generate-notif-endpoints.js",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "allure:gen": "node scripts/allure.js gen",
    "allure:open": "node scripts/allure.js open",
    "allure:clean": "node scripts/allure.js clean",
    "ios:doctor": "npx appium-doctor --ios",
    "ios:version": "npx appium --version",
    "ios:drivers": "npx appium driver list --installed",
    "normalize:web-ids": "node scripts/normalize-web-ids.js"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "baseline-browser-mapping": "^2.9.6",
    "prettier": "^3.5.3",
    "turbo": "^2.5.4"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "npm@11.5.1",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  },
  "overrides": {
    "fastify": "5.4.0"
  }
}
